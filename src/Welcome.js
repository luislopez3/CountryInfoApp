import React from "react";

export default function Welcome() {
    return (
      <div>
        <h1>Welcome!</h1>
        <p>
          This app uses nested routes to display data for all the Countries around
          the World.
        </p>
        <p>To begin, click the "Regions" link.</p>
      </div>
    );
  }